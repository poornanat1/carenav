"""redaction — planned, not yet implemented.

Three-layer PII detection + session-scoped tokenization, applied before any model
call and reversed only on the final reply. See docs/05-redaction.md.
"""
