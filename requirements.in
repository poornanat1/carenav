# Top-level runtime dependencies. Compile with:
#   pip-compile requirements.in -o requirements.txt
# (kept in sync with [project].dependencies in pyproject.toml)

# --- serving ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- schemas / config ---
pydantic>=2.6
pydantic-settings>=2.2

# --- data layer ---
sqlalchemy>=2.0
psycopg[binary]>=3.1
pgvector>=0.2.5

# --- models (Mistral default) ---
mistralai>=2.0

# --- PII / PHI redaction ---
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7

# --- http / resilience ---
httpx>=0.27
tenacity>=8.2

# --- observability ---
opentelemetry-api>=1.24
opentelemetry-sdk>=1.24
structlog>=24.1

# --- misc ---
python-dotenv>=1.0
