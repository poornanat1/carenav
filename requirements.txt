# NOTE: This is the uncompiled fallback so `pip install -r requirements.txt` works
# out of the box. For a fully pinned lock, run `make lock` (pip-compile), which
# rewrites this file with hashes from requirements.in.

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
sqlalchemy>=2.0
psycopg[binary]>=3.1
pgvector>=0.2.5
mistralai>=2.0
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7
httpx>=0.27
tenacity>=8.2
opentelemetry-api>=1.24
opentelemetry-sdk>=1.24
structlog>=24.1
python-dotenv>=1.0
